[pytest]
# Collect only the suite.
testpaths = tests

# A handful of plain `async def` tests carry @pytest.mark.asyncio; every other
# async test is a unittest.IsolatedAsyncioTestCase that needs no plugin.
asyncio_mode = strict
markers =
    asyncio: mark a plain coroutine test to be run by pytest-asyncio
